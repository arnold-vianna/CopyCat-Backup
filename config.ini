[DEFAULT]
source_dir = /home/copycat/Desktop/afolder/
dest_dir = /home/copycat/Desktop/bfolder/
extensions = .txt,.pdf,.jpg,.png,Dockerfile,.zip,.tar,.gz,.bz2  # Optional, comma-separated list of extensions to copy

[logging]  # Optional, for customizing logging behavior
level = INFO  # Logging level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
format = %(asctime)s - %(message)s  # Log message format
datefmt = %Y-%m-%d %H:%M:%S  # Date/time format
